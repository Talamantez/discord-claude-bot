[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    asyncio: mark test as requiring asyncio

# Disable warnings for audioop deprecation
filterwarnings =
    ignore:.*audioop.*:DeprecationWarning
